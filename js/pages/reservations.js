async function render(container, params) {
    container.innerHTML = `
        <a href="#/admin/dashboard">← Back to Dashboard</a>
        <h2>Manage Reservations</h2>
        <p>Reservation management is coming soon.</p>
    `;
}

export default { render };
